{{#if @first}}
<div style="width:33%;align-self:center">
    <!-- Vertical Native -->
    <ins class="adsbygoogle"
        style="display:block"
        data-ad-client="ca-pub-3478521594413822"
        data-ad-slot="5486065474"
        data-ad-format="auto"
        data-full-width-responsive="true"></ins>
    <script>
        (adsbygoogle = window.adsbygoogle || []).push({});
    </script>
</div>
{{/if}}
<article class="post-card {{#if @first}}first-post {{/if}}{{post_class}}{{#unless feature_image}} no-image{{/unless}}">
    {{#if feature_image}}
        <a class="post-card-image-link" href="{{url}}">
            <div class="post-card-image" style="background-image: url({{feature_image}})"></div>
        </a>
    {{/if}}
    <div class="post-card-content">
        <a class="post-card-content-link" href="{{url}}">
            <header class="post-card-header">
                <h2 class="post-card-title">{{title}}</h2>
            </header>
            <section class="post-card-excerpt">
                <p>{{excerpt words="33"}}</p>
            </section>
        </a>
        <footer class="post-card-meta">
            <div class="tag-list">
            {{#if tags.length}}
                {{#foreach tags}}
                    <a href="{{url}}">{{name}}</a>
                {{/foreach}}
            {{/if}}
            </div>

            <ul class="author-list">
            {{#author}}
                <li class="author-list-item">

                    {{#if profile_image}}
                    <a href="{{url}}" class="static-avatar"><img class="author-profile-image" src="{{profile_image}}" alt="{{name}}" /><span class="author-profile-name">{{ name }}</span></a>
                    {{else}}
                    <a href="{{url}}" class="static-avatar author-profile-image">{{> "icons/avatar"}}<span class="author-profile-name">{{ name }}</span></a>
                    {{/if}}
                </li>
            {{/author}}
            </ul>

            <span class="reading-time">{{reading_time}}</span>

        </footer>
    </div>
</article>
